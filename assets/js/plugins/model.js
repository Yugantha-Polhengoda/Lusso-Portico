// const modelViewer = document.querySelector("#threed");

// // Save the default camera orbit
// const defaultOrbit = modelViewer.getAttribute("camera-orbit");

// // Listen for when the user stops interacting with the model
// modelViewer.addEventListener("camera-change", () => {
//   if (!modelViewer.interacting) {
//     // Reset the camera orbit to the default value
//     modelViewer.setAttribute("camera-orbit", defaultOrbit);
//   }
// });
